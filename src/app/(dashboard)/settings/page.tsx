import SettingsView from "@/modules/settings/ui/SettingsView";

export const metadata = {
  title: "Settings | FleexyTrip Admin",
};

export default function SettingsPage(): React.ReactNode {
  return <SettingsView />;
}
