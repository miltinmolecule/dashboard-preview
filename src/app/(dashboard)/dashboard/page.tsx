import DashboardView from "@/modules/dashboard/ui/DashboardView";

export const metadata = {
  title: "Dashboard | FleexyTrip Admin",
};

export default function DashboardPage(): React.ReactNode {
  return <DashboardView />;
}
