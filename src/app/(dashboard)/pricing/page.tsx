import PricingView from "@/modules/pricing/ui/PricingView";

export const metadata = {
  title: "Pricing | FleexyTrip Admin",
};

export default function PricingPage(): React.ReactNode {
  return <PricingView />;
}
