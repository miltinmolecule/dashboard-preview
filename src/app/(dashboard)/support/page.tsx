import SupportView from "@/modules/support/ui/SupportView";

export const metadata = {
  title: "Support | FleexyTrip Admin",
};

export default function SupportPage(): React.ReactNode {
  return <SupportView />;
}
