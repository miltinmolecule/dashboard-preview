import ZonesAndRegionsView from "@/modules/zones-and-regions/ui/ZonesAndRegionsView";

export const metadata = {
  title: "Zones and Regions | FleexyTrip Admin",
};

export default function ZonesAndRegionsPage(): React.ReactNode {
  return <ZonesAndRegionsView />;
}
